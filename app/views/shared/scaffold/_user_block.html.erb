<ul class="nav pull-right">
  <%- if user_signed_in? %>
  <li class="dropdown">
    <a href="#" data-toggle="dropdown-toggle" class="dropdown-toggle">
      <%= t("labels.welcome")%>, <strong><%= current_user.name %></strong> <i class="icon-angle-down"></i>
    </a>
    <ul class="dropdown-menu">
      <li>
        <%= link_to(fa_icon('file-text-alt', text: '用户资料'), edit_user_path(current_user)) %>
      </li>
      <li>
        <%= link_to( fa_icon('signout', text: t("buttons.sign_out")), destroy_user_session_path, :method=>'delete') %>
      </li>
    </ul>
  </li>
  <% else %>
  <li><%= link_to fa_icon('signin', text: t("buttons.sign_in")), :user_session %></li>
  <% end -%>
</ul>
