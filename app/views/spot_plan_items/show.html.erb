<%= render_page_title "#{@spot_plan_item.name}" do |toolbar|
  toolbar << {
    name: "#{t('labels.back_to')}#{t('model.list', :model => SpotPlanItem.model_name.human)}",
    url: spot_plan_items_path,
    options: {class: 'button'}
  }
end %>

<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th>
        属性
      </th>
      <th>
        值
      </th>
    </tr>
  </thead>
  <tbody>
      </tbody>
  <tfoot>

  </tfoot>
</table>
