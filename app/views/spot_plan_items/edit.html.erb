<%= render_page_title "#{t('model.edit', model: SpotPlanItem.model_name.human)} <small>#{t('simple_form.required.text') }</small>" do |toolbar|
  toolbar << {
    name: "#{t('labels.back_to')}#{t('model.list', :model => SpotPlanItem.model_name.human)}",
    url: spot_plan_items_path,
    options: {class: 'button'}
  }
  end %>
<%= render 'form' %>
