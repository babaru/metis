<%= render_page_title "#{t('model.list', model: Client.model_name.human)}" do |toolbar|
  toolbar << {
    name: icon_text('icon-plus-sign', t('model.create', :model => Client.model_name.human)),
    url: new_client_path,
    options: {class: 'button button-primary'}
  }
end %>

<%= render partial: 'shared/icon_grid', locals: {items: @clients.collect {|client| {name: client.name, url: client_projects_path(client), icon: client.logo.url(:thumb)}}, editable: can?(:manage, Client) } %>
