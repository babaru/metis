<%= render_cell(
  :page_title,
  :show,
  data:
  {
    name: "#{t('model.list', model: Space.model_name.human)}",
    toolbar:
    {
      right:
      [
        render_cell(
          :button,
          :show,
          data:
          {
            name: fa_icon('plus-sign', text: t('model.create', :model => Space.model_name.human)),
            url: new_space_path,
            options:
            {
              class: 'btn btn-primary'
            }
          }
        )
      ]
    }
  }
)
%>

<%= render partial: 'spaces_grid' %>
