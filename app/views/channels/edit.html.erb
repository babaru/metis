<%= render_page_title "#{t('model.edit', model: Channel.model_name.human)} <small>#{t('simple_form.required.text') }</small>" do |toolbar|
  toolbar << {
    name: "#{t('labels.back_to')}#{t('model.list', :model => Channel.model_name.human)}",
    url: medium_data_path(@channel.medium_id, 'channel'),
    options: {class: 'button'}
  }
  end %>
<%= render 'form' %>
