<%= render_page_title "<strong>#{@website.name}</strong> #{t('model.list', model: Channel.model_name.human)}" do |toolbar|
  toolbar << {
    name: render_icon_and_text_content('icon-plus-sign', t('model.create', :model => Channel.model_name.human)),
    url: new_website_channel_path(@website),
    options: {class: 'button button-primary'}
  }
end %>

<ul class="nav nav-tabs">
  <li><%= link_to render_icon_and_text_content('icon-trello', ChannelGroup.model_name.human), website_channel_groups_path(@website) %></li>
  <li class="active"><%= link_to render_icon_and_text_content('icon-sitemap', Channel.model_name.human), website_channels_path(@website) %></li>
</ul>

<%= render partial: 'channels_grid' %>
