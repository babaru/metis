<div class="narrow-center-page">

<%= render_cell(
  :page_title,
  :show,
  data:
  {
    name: "编辑 <strong>#{@payment.project.name}</strong> #{Payment.model_name.human}记录 <small>#{t('simple_form.required.text') }</small>",
    toolbar: {
      left:
      [
        render_cell(
          :button,
          :show,
          data:
          {
            name: fa_icon('level-up rotate-270'),
            url: project_payments_path(@payment.project),
            options:
            {
              class: 'btn',
              rel: 'tooltip',
              title: "#{t('labels.back_to')}#{@payment.project.name}#{t('model.list', :model => Payment.model_name.human)}",
            }
          }
        )
      ]
    }
  }
)
%>

<%= render 'form' %>

</div>
