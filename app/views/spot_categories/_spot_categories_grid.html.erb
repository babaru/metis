<%= grid(@spot_categories_grid, :show_filters => :always) do |g|
  g.column name: t('activerecord.attributes.spot_category.name') do |spot_category|
    spot_category.name
  end

  g.column name: t('activerecord.attributes.spot_category.description') do |spot_category|
    truncate(spot_category.description, length: 100)
  end

  if can?(:manage, SpotCategory)

    g.column :html => {class: 'btn-cell'} do |spot_category|
      link_to(icon('icon-edit'), edit_spot_category_path(spot_category), :class => 'btn', :rel => 'tooltip', :title => t('buttons.edit'))
    end

    g.column :html => {class: 'btn-cell'} do |spot_category|
      link_to(icon('icon-trash'), spot_category_path(spot_category), :method => :delete, :confirm => t("confirms.delete"), :class => 'btn btn-danger', :rel => 'tooltip', :title => t('buttons.delete'))
    end

  end
end -%>
