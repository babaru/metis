<%= render_page_title "#{t('model.edit', model: MasterPlanItem.model_name.human)} <small>#{t('simple_form.required.text') }</small>" do |toolbar|
  toolbar << {
    name: "#{t('labels.back_to')}#{t('model.list', :model => MasterPlanItem.model_name.human)}",
    url: master_plan_items_path,
    options: {class: 'button'}
  }
  end %>
<%= render 'form' %>
