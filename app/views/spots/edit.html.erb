<%= render_page_title "#{t('model.edit', model: Spot.model_name.human)} <small>#{t('simple_form.required.text') }</small>" do |toolbar|
  toolbar << {
    name: "#{t('labels.back_to')}#{t('model.list', :model => Spot.model_name.human)}",
    url: website_data_path(@website, 'spot'),,
    options: {class: 'button'}
  }
  end %>
<%= render 'form' %>
