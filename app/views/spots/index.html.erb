<%= render_page_title "#{@website.name}#{t('model.list', model: Spot.model_name.human)}" do |toolbar|
  toolbar << {
    name: render_icon_and_text_content('icon-plus-sign', t('model.create', :model => Spot.model_name.human)),
    url: new_spot_path,
    options: {class: 'button button-primary'}
  }
end %>

<article>
  <div class="padding">
    <ul class="nav nav-tabs">
      <li class="active"><%= link_to render_icon_and_text_content('icon-trello', Spot.model_name.human), website_spots_path(@website) %></li>
      <li><%= link_to render_icon_and_text_content('icon-sitemap', Channel.model_name.human), website_channels_path(@website) %></li>
    </ul>
  </div>
</article>

<%= render partial: 'spots_grid' %>
