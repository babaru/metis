<%= render_page_title "<strong>#{@website.name}</strong> #{t('model.list', model: Spot.model_name.human)}" do |toolbar|
  toolbar << {
    name: t('model.upload', model: Spot.model_name.human),
    url: upload_spots_path(website_id: @website),
    options: {
      class: 'button button-primary'
    }
  }
end %>

<%= render partial: 'spot_category_navs', object: @spot_categories %>

<%= render partial: 'spot_category' %>

<%= render partial: 'spots_grid' if @spots_grid %>
