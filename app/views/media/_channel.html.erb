<%= render_page_title "<strong>#{@medium.name}</strong> #{t('model.list', model: Channel.model_name.human)}" do |toolbar|
  toolbar << {
    name: render_icon_and_text_content('icon-plus-sign', t('model.create', :model => Channel.model_name.human)),
    url: new_medium_channel_path(@medium),
    options: {class: 'button button-primary'}
  }
end %>

<article>
  <div class="padding">
    <ul class="nav nav-tabs">
      <li><%= link_to render_icon_and_text_content('icon-trello', ChannelGroup.model_name.human), medium_data_path(@medium, :channel_group) %></li>
      <li class="active"><%= link_to render_icon_and_text_content('icon-sitemap', Channel.model_name.human), medium_data_path(@medium, :channel) %></li>
    </ul>
  </div>
</article>

<%= render partial: 'channels/channels_grid' %>
