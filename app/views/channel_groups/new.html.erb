<%= render_page_title "#{t('model.create', model: ChannelGroup.model_name.human)} <small>#{t('simple_form.required.text') }</small>" do |toolbar|
  toolbar << {
    name: "#{t('labels.back_to')}#{t('model.list', :model => ChannelGroup.model_name.human)}",
    url: website_channel_groups_path(@website),
    options: {class: 'button'}
  }
  end %>
<%= render 'form' %>
