<%= render_page_title "#{t('model.list', model: ChannelGroup.model_name.human)}" do |toolbar|
  toolbar << {
    name: render_icon_and_text_content('icon-plus-sign', t('model.create', :model => ChannelGroup.model_name.human)),
    url: new_channel_group_path,
    options: {class: 'button button-primary'}
  }
end %>

<%= render partial: 'channel_groups_grid' %>
