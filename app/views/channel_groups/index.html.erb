<%= render_cell(
  :page_title,
  :show,
  data:
  {
    name: "<strong>#{@medium.name}</strong> #{t('model.list', model: ChannelGroup.model_name.human)}",
    toolbar:
    {
      right:
      [
        (render_cell(
          :button,
          :show,
          data:
          {
            name: fa_icon('plus-sign', text: t('model.create', :model => ChannelGroup.model_name.human)),
            url: new_medium_channel_group_path(@medium),
            options:
            {
              class: 'btn btn-primary'
            }
          }
        ) if can?(:manage, ChannelGroup))
      ]
    }
  }
)
%>

<ul class="nav nav-tabs">
  <li class="active"><%= link_to icon_text('icon-trello', ChannelGroup.model_name.human), medium_channel_groups_path(@medium) %></li>
  <li><%= link_to icon_text('icon-sitemap', Channel.model_name.human), medium_channels_path(@medium) %></li>
  <li><%= link_to icon_text('icon-folder-close', SpotCategory.model_name.human), medium_spot_categories_path(@medium) %></li>
  <li><%= link_to icon_text('icon-asterisk', Spot.model_name.human), medium_spots_path(@medium) %></li>
</ul>

<%= render partial: 'channel_groups_grid' %>
