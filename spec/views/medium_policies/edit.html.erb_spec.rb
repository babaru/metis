require 'spec_helper'

describe "medium_policies/edit" do
  before(:each) do
    @medium_policy = assign(:medium_policy, stub_model(MediumPolicy))
  end

  # it "renders the edit medium_policy form" do
  #   render

  #   # Run the generator again with the --webrat flag if you want to use webrat matchers
  #   assert_select "form[action=?][method=?]", medium_policy_path(@medium_policy), "post" do
  #   end
  # end
end
